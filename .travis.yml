language: java
sudo: required
services:
  - docker
install:
  - mvn package
  - mvn docker:build
  - docker run —-name bensonapi -d -p 8080:8080 -t kdbuck/bensonapi
script:
  - docker stop bensonapi