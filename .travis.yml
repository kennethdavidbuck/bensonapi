language: java
sudo: required
services:
  - docker
install:
  # pass git tag
  - mvn package -Dbuild.git.tag=$(git rev-parse HEAD)
  - mvn docker:build
script:
  - docker login -e="$DOCKER_EMAIL" -u="$DOCKER_USERNAME" -p="$DOCKER_PASSWORD"
  - docker push kdbuck/bensonapi
